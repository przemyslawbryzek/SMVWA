<div class="flex flex-row gap-5 items-center p-4 fixed top-0 w-[calc(100%-4rem)] xl:w-3/7 z-60 bg-black">
    <a href="<%= backUrl || '/' %>" class="font-bold p-2 rounded-full hover:bg-stone-900">←</a>
    <p class="text-2xl font-bold text-center"><%= title %></p>
</div>
<div class="flex flex-row gap-5 items-center p-4">
    <a href="<%= backUrl || '/' %>" class="font-bold p-2 rounded-full invisible">←</a>
    <p class="text-2xl font-bold text-center invisible"><%= title %></p>
</div>
